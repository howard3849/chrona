# Chrona v2.5.1

- Simplified Google Sheet controls to Import and Export.
- Replaced the Settings help copy with one compact line and an inline sample-download link.
- Injected the current sample worksheet exported from the maintained Google Sheet.
- Standardized Timeline Data on `Type` and `Group`; removed duplicate `Element Type`, `Category`, and `Role` compatibility paths.
- `Type` uses blank for ordinary events, `period` for Chrona period blocks, and `title` for the dataset title.
- Simplified workbook import to Timeline Data plus optional Config; translations remain inline in Timeline Data.
