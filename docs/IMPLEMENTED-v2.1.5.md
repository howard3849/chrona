# Chrona v2.1.4

Implemented:
- Phone vertical period text is centered.
- Period text surface is opaque and uses the active theme surface color.
- Period text surface is inset to reveal SVG frame and rounded caps.
- iPhone yellow ruler labels restored at the right endpoint: year above, relative years below, yellow text.
- Search expands as one coordinated row on desktop and phone.
- Search result counter remains at the end of the text field.
- Added explicit close (×) control.
- Closing search or clicking outside clears search query, highlight, and dimming.
